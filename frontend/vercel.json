{
  "rewrites": [
    {
      "comment": "(.*) rather than :path* — the named form does not match a path that ends in a slash, so GET /api/addresses/ fell through to the catch-all below and answered with index.html. Code that expected JSON got a page of HTML and read undefined off it.",
      "source": "/api/(.*)",
      "destination": "https://blgomla-api.vercel.app/api/$1"
    },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" },
        { "key": "Content-Security-Policy", "value": "frame-ancestors 'self'" },
        {
          "key": "Strict-Transport-Security",
          "value": "max-age=31536000; includeSubDomains"
        }
      ]
    }
  ]
}
